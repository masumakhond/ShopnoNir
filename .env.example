# Local development (SQLite — works without Supabase)
DATABASE_URL="file:./dev.db"

# Production (Supabase PostgreSQL)
# DATABASE_URL="postgresql://USER:PASSWORD@db.xxxxx.supabase.co:5432/postgres?sslmode=require"

# openssl rand -base64 32
JWT_SECRET="change-this-to-a-long-random-secret"

# Optional: first admin (used by npm run db:seed)
ADMIN_EMAIL="masumbillahakhond@samiti.local"
ADMIN_PASSWORD="Ma$um@1995"
ADMIN_NAME="সমিতি Admin"
